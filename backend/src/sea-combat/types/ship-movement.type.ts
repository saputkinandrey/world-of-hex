export enum ShipMovementPaceType {
  ACCELERATE = 'ACCELERATE',
  DECELERATE = 'DECELERATE',
  SUPPORT = 'SUPPORT',
}

export enum ShipMovementTurnType {
  LEFT = 'LEFT',
  RIGHT = 'RIGHT',
  STRAIGHT = 'STRAIGHT',
}
