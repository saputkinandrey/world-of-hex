# Web Client Agent Instructions

## Goal
- Provide a lightweight Socket.IO client for quick prototyping.

## Scope
- Keep changes inside `web-client/`.

## Implementation
- Use Next.js (App Router), matching the admin setup.
- Keep dependencies minimal (Socket.IO client only unless needed).
- Use MUI for UI work; preserve the web-client color scheme (distinct from admin).
